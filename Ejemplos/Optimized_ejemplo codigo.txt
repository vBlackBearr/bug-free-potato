import raptor_util_Print;
public class Miclase{
   pueden declarar constructores   
  private Ig(int pfd){
  }
  private Igf(int pfdds){
  }
   tiene un metodo principal, para colocar el codigo correspondiente a la programacion
  public void principalMain(){
     int a=1;
     int c=3;
     int e=5;
     If(a>=3 AND false){
     }
	  es un lenguaje que pretende ser sencillo y facil de usar, esto se puede lograr
	  traves de una buena documentacion y de un buen manual de usuario, ademas de una buenta optimizacion del lenguaje
     escribirEnConsola("Hola mundo desde Rex Raptor");
     int x=30;
     string y="hola";
     escribirEnConsola(x);
     int sss;
     sss = 2 / 5;
     float f=6;
     float i=9;
     float j=10;
     escribirEnConsola(y);
     escribirEnConsola(z);
     int z=x+50;
      c;
     string[2] arreglo=("hola");
     escribirEnConsola(z);
      permite combinar ciclos con condicionales, y anidar diferentes funciones
     If(x>=5 AND true OR x==x){
		  char cara='s';
		  char carb='i';
		  char carc='n';
		  char card='o';
          While(true){
          } se pueden colocar comentarios donde el usuario quiera sin ninguna restriccion y
          escribirEnConsola("X es mayor");
          If(true){
               For(int i =0; i<=5;i++){
                    escribirEnConsola(z);
               }
          }
		  Else{
               int j;
               z=x+j+5*5434/44;
               float[5] arregloss=(3.6 + 2);
          }
     }
  }    
     public void escribirEnConsola(char variable){
		   tiene la funcion try-catch para poder evitar errores en el funcionamiento del codigo
          Try{
               escribirEnConsola(z);
			   string cade="hola mundo";
          }
		  Catch(Except e){
               escribirEnConsola(x);
               escribirEnConsola(y);
               escribirEnConsola(z);
          }
		   switch son importantes porque ayudan a crear ahorrar espacio en lineas de codigo
		   colocar una sola funcion switch en lugar de varios if's anidados
          Switch vaz{
               Case 1:
                  escribirEnConsola(x);
               break;
               Case "a":
                  escribirEnConsola(y);
               break;
               Case 'b':
                  escribirEnConsola(f);
               break;
               Default:
                  escribirEnConsola(p);
               break;
          }
     }
     public int sumarNumeros(int xxy, string yy){ 
          int ent = 4;
		   pueden almacenar resultados de operaciones dentro de una variable
		  Switch ent{
               Case 1:
                  escribirEnConsola(x);
               break;
               Case 2:
                  escribirEnConsola(y);
               break;
               Case 3:
                  escribirEnConsola(f);
               break;
               Default:
                  escribirEnConsola(p);
               break;
          }
		   permite retornar valores de variable dentro de un metodo
          return z;
     }
}
